const moduleConfig = {
  key: 'permissions',
  name: 'permissions',
  version: '1.0.0',
  enabled: true,
  dependencies: ['roles', 'auth'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;

