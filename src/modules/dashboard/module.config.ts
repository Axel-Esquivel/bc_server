const moduleConfig = {
  key: 'dashboard',
  name: 'dashboard',
  version: '1.0.0',
  enabled: true,
  dependencies: ['auth', 'companies', 'organizations'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;


