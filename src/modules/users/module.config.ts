const moduleConfig = {
  key: 'users',
  name: 'users',
  version: '1.0.0',
  enabled: true,
  dependencies: ['organizations'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;



