const moduleConfig = {
  key: 'organizations',
  name: 'organizations',
  version: '1.0.0',
  enabled: true,
  dependencies: ['users', 'auth'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;


