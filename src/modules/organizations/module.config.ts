const moduleConfig = {
  key: 'organizations',
  name: 'organizations',
  version: '1.0.0',
  enabled: true,
  dependencies: ['module-loader'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;


