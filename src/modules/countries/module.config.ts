const moduleConfig = {
  key: 'countries',
  name: 'countries',
  version: '1.0.0',
  enabled: true,
  dependencies: ['auth'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;

