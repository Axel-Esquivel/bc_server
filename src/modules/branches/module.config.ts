const moduleConfig = {
  key: 'branches',
  name: 'branches',
  version: '1.0.0',
  enabled: true,
  dependencies: ['auth', 'companies'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;



