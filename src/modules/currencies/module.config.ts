const moduleConfig = {
  key: 'currencies',
  name: 'currencies',
  version: '1.0.0',
  enabled: true,
  dependencies: ['auth', 'organizations'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;


