const moduleConfig = {
  key: 'companies',
  name: 'companies',
  version: '1.0.0',
  enabled: true,
  dependencies: ['users', 'auth', 'organizations'],
  isSystem: true,
  isInstallable: false,
};

export default moduleConfig;



